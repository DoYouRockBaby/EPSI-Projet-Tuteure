@using Carsale.DAO.Models

@model FuelWholesaler

@{
    ViewBag.Title = "Supprimer " + Model.Name;
}

<h2>Supprimer @Model.Name</h2>


<h3>Voulez vous vraiment supprimer ce grossiste ?</h3>
<div>
    <h4>Pièce</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Address)
        </dd>
    </dl>

    @using (Html.BeginForm("DeleteConfirmed", "FuelWholesaler", new { id = Model.Id }, FormMethod.Post, null))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Supprimer" class="btn btn-primary" />
        </div>
        <div>
            @Html.ActionLink("Retour a la liste des grossistes", "List")
        </div>
    }
</div>