@using Carsale.DAO.Models
@using Carsale.ViewModels

@model CreateVehicleViewModel
@{
    ViewBag.Title = "Create";
}

<h2>
    Vehicle
</h2>

@using (Html.BeginForm("Create", "Vehicle", FormMethod.Post, new { id = "create", @Class = "form-horizontal" }))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Vehicle.Matriculation)
        @Html.TextBoxFor(m => m.Vehicle.Matriculation, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Vehicle.Matriculation)
    </div>
    
    <div class="form-group">
        @Html.LabelFor(m => m.Brands)
        @Html.DropDownListFor(m => m.Vehicle.BrandId, Model.Brands.Select(brand => new SelectListItem
        {
            Value = brand.Id.ToString(),
            Text = brand.Name
        }), new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Brands)

        <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapse-brand-form" aria-expanded="false" aria-controls="collapse-brand-form">
            Nouvelle Marque
        </button>

        <div class="collapse" id="collapse-brand-form">
            @using (Ajax.BeginForm("Create", "Brand", new AjaxOptions { }))
            {

                <div class="form-group">
                    @Html.LabelFor(m => m.Brand.Name)
                    @Html.TextBoxFor(m => m.Brand.Name, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Brand.Name)
                </div>
                <div class="col-md-4">
                    <button type="button" class="btn btn-default">Validate</button>
                </div>
            }
        </div>
    </div>
    
    <div class="form-group">
        @Html.LabelFor(m => m.Vehicle.Power)
        @Html.TextBoxFor(m => m.Vehicle.Power, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Vehicle.Power)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Vehicle.Model)
        @Html.TextBoxFor(m => m.Vehicle.Model, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Vehicle.Model)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.VehicleColor)
        @Html.DropDownListFor(m => m.VehicleColor, (Model.VehicleColor as IEnumerable<VehicleColor>).Select(e => new SelectListItem() { Text = e.ToString(), Value = e.ToString() }).AsEnumerable(), new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.VehicleColor)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Status)
        @Html.DropDownListFor(m => m.Status, (Model.Status as IEnumerable<StatusVehicle>).Select(e => new SelectListItem() { Text = e.ToString(), Value = e.ToString() }).AsEnumerable(), new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Status)
    </div>
    <div class="col-md-4">
        <button type="submit" class="btn btn-default">Validate</button>
    </div>
}
    

